---
# Policy for testenv time synchronization validation.
# Note: monitor_address is expected to be passed from the calling Play (checks.yml Play 2)

time_sync_monitor_ip: "{{ monitor_address }}"
time_sync_max_offset_seconds: 2.0
